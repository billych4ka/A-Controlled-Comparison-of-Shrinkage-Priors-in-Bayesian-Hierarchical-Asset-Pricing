# Exact environment used for every result reported in the dissertation.
# Pinned from `pip freeze` on the interpreter the runs were executed with:
#
#   CPython 3.13.7 (python.org framework build), macOS 26.5.2 /
#   Darwin 25.5.0, arm64.
#
# Reproduce with:
#
#   python3.13 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# The seven packages under DIRECT are the ones the project imports; the
# TRANSITIVE block is their full dependency closure, pinned so that a
# reinstall cannot silently pick up a different numba, xarray or arviz-stats.
# The pins are exact (==) rather than floors: MCMC output is only bit-stable
# within a fixed PyTensor/NumPy pair, and arviz 1.2.0 in particular is the
# version the diagnostics are written against -- az.bfmi raises a TypeError
# on exactly this version, which is why E-BFMI is computed by hand in
# src/nuts/horseshoe.py and src/nuts/regularised_horseshoe.py, and why
# az.rhat/az.ess are called only from tests/.

# --- DIRECT -----------------------------------------------------------------
numpy==2.4.2
scipy==1.17.1
pandas==3.0.0
pymc==6.2.0
pytensor==3.2.3
arviz==1.2.0
matplotlib==3.10.8

# --- TRANSITIVE -------------------------------------------------------------
arviz-base==1.2.0
arviz-plots==1.2.0
arviz-stats==1.2.0
cachetools==6.2.6
cloudpickle==3.1.2
contourpy==1.3.3
cycler==0.12.1
filelock==3.32.0
fonttools==4.61.1
kiwisolver==1.4.9
lazy-loader==0.5
llvmlite==0.47.0
markdown-it-py==4.2.0
mdurl==0.1.2
numba==0.65.1
packaging==25.0
pillow==12.1.0
Pygments==2.19.2
pyparsing==3.3.2
python-dateutil==2.9.0.post0
rich==15.0.0
setuptools==80.9.0
six==1.17.0
threadpoolctl==3.6.0
typing_extensions==4.15.0
xarray==2026.7.0
